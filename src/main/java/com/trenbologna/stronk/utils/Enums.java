package com.trenbologna.stronk.utils;

public class Enums {
    public enum Intensity {
        WARM_UP,
        FAILURE,
        DROP_SET
    }

    public enum WeightUnit {
        METRIC,
        IMPERIAL
    }
}
